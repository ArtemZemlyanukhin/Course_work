# Курсовая работа
Цель данной работы - алгоритмизация и написание кода на языке СИ для табулирования кусочной функции f(x), использующую многоальтернативный выбор решения:
<img width="356" height="174" alt="image" src="https://github.com/user-attachments/assets/c97d000d-6fc9-423e-91a8-76d8bc04bd86" />


### График функции f(x) 
<img width="749" height="652" alt="image" src="https://github.com/user-attachments/assets/64ad7479-1187-4ec7-b6b4-76bf1f1e30c8" />



## Задачи, которые необходимо решить в данной работе:
Создать интерфейс программы, который должен обеспечивать выполнение операций:
 - Значение – f(x) в точке;
 - Таблица – x → f(x) на интервале;
 - Определённый интеграл на интервале методом средних прямоугольников;
 - Определение типа монотонности функции на интервале
 - Производная f’(x) в точке;
 - Запись значения функции в точке в файл
 - Запись значений функции на интервале



## 📁 Структура проекта

├── .gitattributes

├── .gitignore

├── main_file.c

├── func.h

├── func.c

├── Kursovaya_Rabota_1.sln

├── Kursovaya_Rabota_1.vcxproj

├── Kursovaya_Rabota_1.vcxproj.filters

├── funci.png

├── grafic1.png

├── grafic2.png

├── grafic3.png

└── README.md


## 🔧 Реализованные функции и структуры




### 1. 'typedef struct'
- **Назначение**: Структура для хранения входных параметров анализа функции.
- **Поля структуры**:
  - 'start' - начало интервала анализа
  - 'finish' - конец интервала анализа
  - 'step' - шаг для построения таблицы значений
  - 'precision' - точность вычислений для поиска, производной и интеграла
- **Название**: 'input_params_t'




### 2. `input_params_t get_input_params(void)`
- **Назначение**: Запрашивает у пользователя параметры анализа функции.
- **Запрашивает**:
  - 'start' - начало
  - 'finish' - конец интервала анализа
  - 'step' - шаг для таблицы значений
  - 'precision' - точность вычислений
- **Возвращает**: структуру input_params_t с введенными пользователем значениями




### 3. `double sigma(double x, int n)`
- **Назначение**: Вычисляет сигма-сумму  для аппроксимации функции.
- **Параметры**:
  - 'x' - аргумент функции
  - 'n' - количество членов ряда
- **Возвращает**: сумму ряда
 


### 4. `double f(double x)`
- **Назначение**: Вычисляет значение кусочно-заданной функции f(x).
- **Параметры**:
  - 'x' - аргумент функции
- **Возвращает**: значение функции f(x)





### 5. `int build_table(input_params_t params)`
- **Назначение**: Строит таблицу значений функции на заданном интервале с заданным шагом.
- **Параметры**:
  - 'params' - структура с параметрами (используются start, finish, step)
- **Возвращает**: 1, если в таблице есть хотя бы одно определенное значение функции, 0, если все значения не определены



### 6. `double find_x_by_y(input_params_t params, double y)`
- **Назначение**: Находит первое значение X, при котором f(X) равно Y с заданной точностью.
- **Параметры**:
  - 'params' - структура с параметрами (используются start, finish, precision)
  - 'y' - искомое значение функции
- **Возвращает**: найденное значение X или 0, если значение не найдено



### 7. `int count_x_by_y(input_params_t params, double y)`
- **Назначение**: Подсчитывает количество значений X на интервале, при которых f(X) равно Y с заданной точностью.
- **Параметры**:
  - 'params' - структура с параметрами (используются start, finish, precision)
  -  'y' - искомое значение функции
- **Возвращает**: количество найденных значений X


### 8. `double calculate_derivative(double x, double precision)`
- **Назначение**: Вычисляет производную функции в точке с использованием метода конечных разностей.
- **Параметры**:
  - 'x' - точка, в которой вычисляется производная
  - 'precision' - точность, она же величина приращения для вычисления производной
- **Возвращает**: приближенное значение производной f'(x)



### 9. `double calculate_integral(input_params_t params)`
- **Назначение**: Вычисляет определенный интеграл функции на заданном интервале методом прямоугольников.
- **Параметры**:
  - 'params' - структура с параметрами (используются start, finish, precision)
- **Возвращает**: приближенное значение интеграла f(x)dx от start до finish
 
