# Курсовой проект
Цель данной работы - алгоритмизация и написание кода на языке СИ для табулирования кусочной функции f(x), использующую многоальтернативный выбор решения:
<img width="356" height="174" alt="image" src="https://github.com/user-attachments/assets/c97d000d-6fc9-423e-91a8-76d8bc04bd86" />


### График функции f(x) 
<img width="749" height="652" alt="image" src="https://github.com/user-attachments/assets/64ad7479-1187-4ec7-b6b4-76bf1f1e30c8" />



## Задачи, которые необходимо решить в данной работе:
Создать интерфейс программы, который должен обеспечивать выполнение операций:
 - Значение – f(x) в точке;
 - Таблица – x → f(x) на интервале;
 - Определённый интеграл на интервале методом средних прямоугольников;
 - Определение типа монотонности функции на интервале
 - Производная f’(x) в точке;
 - Запись значения функции в точке в файл
 - Запись значений функции на интервале в файл



## 📁 Структура проекта

├── .gitattributes

├── .gitignore

├── Course_work1.c

├── Functions.h

├── Functions.c

├── Course_work.sln

├── Course_work.vcxproj

├── Course_work.vcxproj.filters

├── Function_f(x).png

├── Graph_f(x).png

└── README.md


## 🔧 Реализованные функции и структуры




### 1. 'typedef enum'
- **Назначение**: Перечисление для классификации монотонности функции.
- **Константы**:
  - 'CONSTANT' - Функция постоянна на интервале
  - 'INCREASING' - Функция строго возрастает на интервале
  - 'DECREASING' - Функция строго убывает на интервале
  - 'NOT_DECREASING' - Функция не убывает на интервале(возрастает или постоянна)
  - 'NOT_INCREASING' - Функция не возрастает на интервале(убывает или постоянна)
  - 'NOT_MONOTON' - Функция немонотонна на интервале(имеет точки экстремума)
- **Название**: 'MONOTONTYPE'




### 2. `double funct(double x);`
- **Назначение**: Вычисление значения функции в точке.
- **Запрашивает**:
  - 'x' - значение аргумента
- **Возвращает**: значение функции в точке x




### 3. `double integral(double a, double b);`
- **Назначение**: Вычисление определённого интеграла методом средних прямоугольников
- **Параметры**:
  - 'a' - нижний предел интегрирования
  - 'b' - верхний предел интегрирования
- **Возвращает**: значение определённого интеграла на интервале
 


### 4. `MONOTONTYPE monoton(double a, double b);`
- **Назначение**: Определение типа монотонности функции на интервале.
- **Параметры**:
  - 'a' - начало интервала
  - 'b' - конец интервала
- **Возвращает**: константа типа MONOTONTYPE





### 5. `double proizvod(double x);`
- **Назначение**: Вычисление производной в точке.
- **Параметры**:
  - 'x' - точка, в которой вычисляется производная
- **Возвращает**: значение производной в точке x



### 6. `int append_to_file(char* fname, double s, double s2);`
- **Назначение**: Запись результата вычисления функции в точке в файл
- **Параметры**:
  - 'fname' - имя файла
  - 's' - значение аргумента x
  - 's2' - значение функции f(x)
- **Возвращает**: 1 при успехе, 0 при ошибке



### 7. `int append_interval_into_file(char* fname, double a, double b, double st);`
- **Назначение**: 
- **Параметры**:
  - 'fname' - имя файла
  -  'a' - начало интервала
  -  'b' - конец интервала
  -  'st' - шаг табулирования
- **Возвращает**: 1 при успехе, 0 при ошибке

